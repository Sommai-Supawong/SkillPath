You are working on the existing SkillPath project.

Your task is to improve the Roadmap experience by adding a VISUAL INTERACTIVE ROADMAP DIAGRAM that clearly shows:

- what the learner should study first
- what skill comes next
- prerequisite relationships
- branching learning paths
- completed/current/available/locked skills
- current level → required/target level
- estimated learning hours

IMPORTANT:

DO NOT use AI, LLM, Gemini, OpenAI, or any generative model to determine the roadmap.

The roadmap ordering must continue to come entirely from the existing deterministic backend logic:

- GapAnalyzer
- RoadmapEngine
- RoadmapStrategy
- prerequisite graph
- priority scoring
- topological ordering
- dependency/cycle validation

The frontend should only VISUALIZE the roadmap calculated by the backend.

The objective is:

TURN THE CURRENT ROADMAP INTO A CLEAR INTERACTIVE LEARNING GRAPH SIMILAR IN CONCEPT TO A DEVELOPER ROADMAP DIAGRAM, WHILE PRESERVING THE EXISTING OOAD/OOP ARCHITECTURE.

Do not redesign the entire project.

Focus only on improving the Roadmap visualization and supporting data flow.

---

# 1. BEFORE CODING

First inspect the existing project completely.

Read:

SkillPath/docs/Document.md

Also inspect:

- current RoadmapEngine
- GapAnalyzer
- roadmap strategies
- prerequisite implementation
- Roadmap database models
- roadmap API routes
- roadmap page
- SkillIcon / SkillBadge / technology logo system if already implemented
- frontend styling conventions
- current Tailwind configuration
- current dependencies

Do not rewrite working roadmap logic.

Reuse existing code wherever possible.

The backend roadmap remains the SOURCE OF TRUTH.

---

# 2. TARGET EXPERIENCE

The Roadmap page should visually communicate a learning sequence similar to:

Web Foundations
      │
      ├──────────────┐
      ▼              ▼
    HTML            CSS
      │              │
      └──────┬───────┘
             ▼
        JavaScript
             │
             ▼
        TypeScript
             │
             ▼
           React
             │
       ┌─────┴──────┐
       ▼            ▼
    Next.js       Testing
       │
       ▼
   REST APIs
       │
       ▼
    Backend
       │
       ▼
   PostgreSQL
       │
       ▼
      Docker

However:

DO NOT hard-code this sequence.

The real diagram must be dynamically generated from the selected learner's:

- target career
- skill gaps
- roadmap strategy
- prerequisite relationships
- current skill levels
- roadmap generated by RoadmapEngine

Different careers must naturally produce different diagrams.

Examples:

Frontend Developer
Data Scientist
Cloud Engineer
AI Engineer
Full Stack Developer

must all use the same reusable roadmap visualization system.

---

# 3. USE REACT FLOW

Use React Flow / `@xyflow/react` for the interactive roadmap visualization if the project does not already contain an equivalent graph library.

Install only if necessary:

npm install @xyflow/react

Use React Flow for:

- nodes
- edges
- arrows
- pan
- zoom
- fit view
- responsive graph
- custom skill nodes

Do not build the complete graph engine manually using absolute-positioned divs.

---

# 4. ROADMAP GRAPH DATA

The frontend should receive structured graph-compatible data.

Preferred structure:

{
  "roadmap_id": 1,
  "career": {
    "id": 1,
    "title": "Frontend Developer"
  },
  "strategy": "balanced",
  "readiness": 57.4,
  "weekly_hours": 8,
  "nodes": [
    {
      "id": "javascript",
      "skill_id": 5,
      "name": "JavaScript",
      "skill_type": "Programming Language",
      "icon_key": "javascript",
      "icon_kind": "simple-icons",
      "current_level": 2,
      "target_level": 5,
      "estimated_hours": 24,
      "priority_score": 19.5,
      "status": "current",
      "order": 1
    },
    {
      "id": "react",
      "skill_id": 6,
      "name": "React",
      "skill_type": "Library",
      "icon_key": "react",
      "icon_kind": "simple-icons",
      "current_level": 1,
      "target_level": 4,
      "estimated_hours": 28,
      "priority_score": 15,
      "status": "locked",
      "order": 2
    }
  ],
  "edges": [
    {
      "id": "javascript-react",
      "source": "javascript",
      "target": "react",
      "type": "prerequisite"
    }
  ]
}

Do not duplicate business logic in the frontend.

If the current roadmap API already provides enough information, transform it cleanly.

If it does NOT contain enough data for graph visualization, add a backend representation such as:

GET /api/roadmaps/{id}/graph

or extend the existing roadmap response.

Choose the option that best fits the current architecture.

---

# 5. GRAPH GENERATION MUST BE BACKEND-AWARE

The diagram must represent actual prerequisite relationships.

Example:

JavaScript
→ React
→ Next.js

The UI must not visually produce:

Next.js
→ React
→ JavaScript

if the backend dependency graph says otherwise.

RoadmapEngine and dependency resolution remain authoritative.

If the roadmap contains independent skills, show branching paths.

Example:

             JavaScript
            /          \
           ▼            ▼
        React        Testing
           │
           ▼
        Next.js

Do not force every roadmap into one straight vertical list.

---

# 6. SKILL NODE DESIGN

Create a reusable custom node component.

Recommended component:

RoadmapSkillNode.tsx

Node layout should approximately contain:

┌───────────────────────────┐
│ [Technology Logo]         │
│                           │
│ JavaScript                │
│ Programming Language      │
│                           │
│ Current        Target     │
│   2       →       5       │
│                           │
│ 24 hrs estimated          │
└───────────────────────────┘

Keep it compact.

Do not create huge cards that make the roadmap difficult to scan.

Use the existing SkillIcon / SkillIdentity system if available.

Technology logos should help users recognize:

HTML
CSS
JavaScript
TypeScript
React
Next.js
Python
FastAPI
PostgreSQL
Docker
AWS
Kubernetes
Terraform
PyTorch
etc.

For conceptual skills without brand logos, use the project's generic icon system.

---

# 7. NODE STATES

Support clear roadmap states.

Required states:

COMPLETED

CURRENT

AVAILABLE

LOCKED

CRITICAL GAP

Visual behavior should be subtle.

Example concept:

Completed
✓ check indicator
slightly reduced emphasis

Current
stronger border
small "CURRENT" label

Available
normal node

Locked
reduced opacity
small lock indicator

Critical Gap
small warning indicator
slightly stronger emphasis

Do NOT use extreme colors.

Do NOT make every status a saturated glowing color.

The UI should remain calm and professional.

---

# 8. VISUAL STYLE

The desired design direction is:

Modern
Minimal
Technical
Professional
Developer-tool inspired

Think:

GitHub
Linear
Vercel
Raycast
modern developer dashboards

Do NOT create an exaggerated "AI dashboard".

Avoid:

- neon purple gradients
- cyan glow everywhere
- excessive glassmorphism
- giant blurred gradient blobs
- excessive rounded floating cards
- AI sparkle icons everywhere
- rainbow gradients
- over-animated backgrounds
- unnecessary 3D
- futuristic sci-fi styling

The visual design should feel like a serious developer learning platform.

---

# 9. COLOR DIRECTION

Respect the existing SkillPath theme.

If it is currently dark, keep a restrained dark interface.

Example direction:

Background:
#0C0C0E

Secondary surface:
#121214

Node:
#18181B

Border:
rgba(255,255,255,0.10)

Primary text:
#F5F5F5

Secondary text:
#A1A1AA

Accent:
use the existing application accent color.

Technology brand colors may be used only for icons.

Do not color entire cards using technology brand colors.

---

# 10. ROADMAP PAGE STRUCTURE

Recommended layout:

------------------------------------------------

Frontend Developer

Your personalized learning roadmap

57% Career Readiness

Balanced Strategy
8 hrs/week

------------------------------------------------

ROADMAP

                  [ HTML ]
                     │
                     ▼
                  [ CSS ]
                     │
                     ▼
              [ JavaScript ]
                     │
             ┌───────┴────────┐
             ▼                ▼
        [ TypeScript ]    [ Testing ]
             │
             ▼
          [ React ]
             │
             ▼
         [ Next.js ]

------------------------------------------------

Progress
3 / 9 skills completed

Estimated remaining time
7 weeks

------------------------------------------------

Do not show unnecessary decorative sections.

---

# 11. ROADMAP HEADER

At the top of the roadmap include:

Career name

Career readiness

Current strategy

Weekly study hours

Estimated completion

Progress

Example:

Frontend Developer

57% Ready

Balanced
8 hrs/week
7 weeks estimated
3 / 9 skills completed

Keep this compact.

---

# 12. GRAPH CONTROLS

Provide useful graph controls:

Fit View

Zoom In

Zoom Out

Reset / Center

Optional:

Fullscreen Roadmap

Do not clutter the interface.

React Flow's built-in controls may be used but style them to match SkillPath.

---

# 13. RESPONSIVE BEHAVIOR

Desktop:

Prefer left-to-right or top-to-bottom graph layout depending on readability.

Tablet:

Allow pan and zoom.

Mobile:

Do NOT try to squeeze the entire roadmap into the viewport.

Use a vertical-first layout.

Allow scrolling/panning.

Nodes should remain readable.

---

# 14. AUTO LAYOUT

Do not manually hard-code x/y coordinates for each career.

Create a graph layout function.

Example:

lib/roadmap/layoutRoadmap.ts

The layout should use:

- roadmap order
- prerequisite depth
- graph branches

You may use:

Dagre

or another lightweight deterministic layout library if helpful.

Example:

npm install @dagrejs/dagre

Only add it if needed.

Expected behavior:

root/foundation skills
→ earlier layer

dependent skills
→ later layer

independent skills
→ parallel branches

The layout must remain deterministic.

Reloading the same roadmap should not randomly reposition nodes.

---

# 15. ROADMAP LEVEL / LAYER CALCULATION

Conceptually calculate:

Layer 0
Foundation skills

Layer 1
Skills unlocked by Layer 0

Layer 2
Skills unlocked by Layer 1

Layer 3
Advanced dependent skills

This should produce visually meaningful diagrams.

Example:

Layer 0:
HTML
CSS

Layer 1:
JavaScript

Layer 2:
TypeScript
React

Layer 3:
Next.js

Do not reinterpret prerequisites in the frontend.

Use dependency relationships supplied by the backend.

---

# 16. EDGE DESIGN

Edges should be simple.

Use:

thin lines

clear arrowheads

subtle animation only where useful

Completed → Current path may optionally receive slightly stronger emphasis.

Avoid:

neon glowing edges

large animated particles

constant distracting animation

Edge meaning:

A → B

means:

A should be learned before B.

---

# 17. NODE INTERACTION

When the user clicks a skill node, open a side panel or details panel.

Example:

React

Library

Current Level
1 / 5

Target Level
4 / 5

Gap
3 levels

Estimated Study
28 hours

Prerequisites
✓ JavaScript

Unlocks
→ Next.js
→ React Testing

Learning Resources

[Open React Documentation]

If existing roadmap progress controls exist, reuse them.

Do not create an AI explanation.

Everything should come from stored SkillPath data.

---

# 18. COMPLETING A SKILL

If the current application allows roadmap progress updates:

when the learner completes or improves a skill:

update the backend

then:

recalculate readiness

recalculate roadmap

refresh graph

update node states

Example:

Before:

JavaScript
Current 2
Target 5

After update:

JavaScript
Current 5
Target 5
✓ Completed

React becomes:

Locked
→ Available / Current

The graph should visually communicate the change.

---

# 19. ANIMATION

Use animation carefully.

Good:

node fade/slide when graph loads

smooth node position changes

subtle progress transitions

smooth edge appearance

Bad:

continuous floating cards

glowing nodes

large scale animations

particle systems

animated gradient backgrounds

Keep motion functional.

Use existing Framer Motion only if already installed.

Do not add it purely for decorative animation.

---

# 20. ROADMAP STRATEGY

The diagram must work with existing strategies:

Balanced

Fast Track

Foundation First

When the strategy changes:

generate/recalculate the roadmap using the backend

then update the diagram.

Do not simply rearrange the same nodes in the frontend.

The backend strategy result remains authoritative.

---

# 21. FILTER OPTIONAL NON-ESSENTIAL SKILLS

If the roadmap becomes visually too large, support a simple view mode:

Core Path

Full Roadmap

Core Path:
show required/current roadmap steps.

Full Roadmap:
show all relevant prerequisite relationships.

Only implement this if the current data structure supports it cleanly.

Do not hide important required skills.

---

# 22. CAREER-SPECIFIC ROADMAPS

Test visual quality with at least:

Frontend Developer

Full Stack Developer

Software Engineer

AI Engineer

Machine Learning Engineer

Generative AI Engineer

Data Scientist

Data Engineer

DevOps Engineer

Cloud Engineer

The graph renderer must be generic.

Do NOT create:

if career == "Frontend Developer":
    ...

if career == "AI Engineer":
    ...

All careers must use the same graph renderer.

---

# 23. EXAMPLE DIFFERENT ROADMAP SHAPES

Frontend example:

HTML
  │
CSS
  │
JavaScript
  │
React
  │
Next.js

AI example:

Python
  │
  ├── NumPy
  │
  └── Pandas
       │
       ▼
Data Preprocessing
       │
       ▼
Machine Learning
       │
       ▼
Deep Learning
       │
       ▼
PyTorch

Cloud example:

Linux ───────┐
             ▼
Networking → Cloud Fundamentals
                    │
          ┌─────────┼──────────┐
          ▼         ▼          ▼
         AWS      Docker      IAM
                    │
                    ▼
                Kubernetes
                    │
                    ▼
                 Terraform

Actual output must come from SkillPath data, not these examples.

---

# 24. COMPONENT STRUCTURE

Suggested structure:

frontend/components/roadmap/

RoadmapGraph.tsx

RoadmapSkillNode.tsx

RoadmapEdge.tsx

RoadmapHeader.tsx

RoadmapProgress.tsx

RoadmapNodeDetails.tsx

RoadmapControls.tsx

frontend/lib/roadmap/

buildRoadmapGraph.ts

layoutRoadmap.ts

roadmapTypes.ts

Adapt this to the existing project conventions.

Do not unnecessarily create many tiny files.

---

# 25. DATA TYPES

Create proper TypeScript types.

Example:

type RoadmapNodeStatus =
  | "completed"
  | "current"
  | "available"
  | "locked"
  | "critical";

interface RoadmapGraphNode {
  id: string;
  skillId: number;
  name: string;
  skillType?: string;
  iconKey?: string;
  iconKind?: string;
  currentLevel: number;
  targetLevel: number;
  estimatedHours: number;
  status: RoadmapNodeStatus;
  priorityScore?: number;
  order: number;
}

interface RoadmapGraphEdge {
  id: string;
  source: string;
  target: string;
  type: "prerequisite";
}

Keep frontend types aligned with backend schemas.

---

# 26. BACKEND RESPONSIBILITY

The backend owns:

career requirements

skill gaps

priority scores

roadmap strategy

roadmap ordering

prerequisites

dependency validation

cycle detection

target levels

estimated learning hours

progress

The frontend owns:

graph rendering

visual layout

interaction

responsive behavior

node presentation

Do not move business rules into React components.

---

# 27. NO AI

This task explicitly requires:

NO OpenAI

NO Gemini

NO Claude

NO AI API

NO generated roadmap text

NO prompt engineering

NO LLM-based ordering

Do not add an AI dependency.

The deterministic RoadmapEngine is enough.

The roadmap diagram is a VISUALIZATION feature, not an AI feature.

---

# 28. FALLBACK

If React Flow cannot render for any reason:

provide a graceful roadmap list fallback.

Example:

1. JavaScript
2. TypeScript
3. React
4. Testing
5. Next.js

Do not leave the user with a blank page.

---

# 29. LOADING / ERROR / EMPTY STATES

Add proper states.

Loading:

Preparing your roadmap...

Empty:

Your current skills already meet the selected career requirements.

Error:

Unable to load roadmap.

Cycle error:

The skill dependency configuration contains a cycle.

Do not expose raw stack traces.

---

# 30. PERFORMANCE

Avoid unnecessary rerenders.

Memoize node types.

Do not repeatedly rebuild graph data during pan/zoom.

Do not fetch backend data separately for every node.

Load the roadmap data as one graph response where possible.

---

# 31. ACCESSIBILITY

Skill names must always be visible as text.

Do not rely solely on logos.

Ensure:

keyboard accessible controls

sufficient contrast

clear focus states

readable node text

accessible buttons

---

# 32. TESTING

Backend tests should continue passing.

Add tests only where backend graph serialization is introduced.

Verify:

prerequisite edges are correct

no duplicate graph nodes

no duplicate graph edges

completed skills receive correct state

roadmap ordering remains unchanged

cycle handling still works

Frontend typecheck must pass.

Production build must pass.

---

# 33. MANUAL TEST SCENARIO

Use at least one learner profile with:

Target:
Full Stack Developer

Example existing skill levels:

HTML = 4
CSS = 4
JavaScript = 2
TypeScript = 1
React = 1
Next.js = 0
Python = 2
FastAPI = 1
SQL = 2
Docker = 0

Generate:

Balanced Roadmap

Verify that:

HTML/CSS can appear completed where appropriate.

JavaScript becomes an early/current step.

React does not incorrectly appear before its prerequisite.

Next.js appears after React.

Backend-related skills appear according to real requirements and dependencies.

Graph branches correctly where multiple independent paths exist.

---

# 34. SECOND MANUAL TEST

Target:

AI Engineer

Verify a graph resembling the real dependency data, such as:

Python
↓
NumPy / Pandas
↓
Statistics / Data Preparation
↓
Machine Learning
↓
Deep Learning
↓
Deployment / MLOps

Again:

do not hard-code this sequence.

Verify it from the existing database prerequisite data.

---

# 35. FINAL QUALITY CHECK

After implementation:

run backend tests

run frontend typecheck

run frontend build

start both backend and frontend

test multiple roadmap strategies

test at least 5 careers

test desktop

test tablet

test mobile

test node click interaction

test graph zoom/pan

test progress update

test roadmap recalculation

test missing logo fallback

test empty roadmap

test error state

fix all discovered issues

---

# 36. COMPLETION REPORT

At the end provide:

- files created
- files changed
- packages installed
- API changes
- graph data model
- React Flow implementation
- auto-layout approach
- custom node implementation
- node status logic
- responsive behavior
- careers manually tested
- backend test result
- frontend typecheck result
- production build result
- remaining limitations

Do not perform a full application redesign.

Do not add AI.

The final objective is:

CREATE A BEAUTIFUL, CLEAR, INTERACTIVE, DETERMINISTIC SKILL ROADMAP DIAGRAM THAT SHOWS THE LEARNER EXACTLY WHAT TO LEARN FIRST, WHAT COMES NEXT, AND WHICH SKILLS DEPEND ON OTHER SKILLS.

The roadmap must remain driven entirely by SkillPath's existing OOAD/OOP backend logic.